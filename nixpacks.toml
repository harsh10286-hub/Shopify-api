[phases.setup]
nixPkgs = ["python311", "libcurl", "cffi", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn shopify_api:app --host 0.0.0.0 --port $PORT"