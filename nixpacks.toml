[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "curl", "pkg-config"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn shopify_api:app --host 0.0.0.0 --port $PORT"
